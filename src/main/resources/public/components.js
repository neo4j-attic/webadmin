/**
 * Component imports. These are the components morpheus is shipped with. In
 * production, this file will be re-generated as new components are added and
 * removed.
 */

var morpheus = morpheus || {};
morpheus.componentList = [ 
        'morpheus.overview',
        'morpheus.server.lifecycle', 
        'morpheus.server.dashboard',
        'morpheus.server.config'];